{
    "builds": [
        {
          "src": "package.json",
          "use": "@vercel/static-build",
          "config": {
            "buildCommand": "npm run dev",
            "devCommand": "npm run dev:server"
          }
        }
      ]
}